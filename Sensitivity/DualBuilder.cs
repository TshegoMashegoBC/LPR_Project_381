using LPR381Solver.Core;

namespace LPR381Solver.Sensitivity
{
    public class DualBuilder
    {
        public LPModel BuildDual(LPModel primal)
        {
            // Placeholder implementation.
            // For now return a cloned model so the project compiles.
            // Replace later with full dual construction logic.

            return primal.Clone();
        }
    }
}be